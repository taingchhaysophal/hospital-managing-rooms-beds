name: hospital_management_system
description: A simple hospital management system (student/mobile project)
publish_to: 'none' # not published
version: 0.0.1+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.21.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/
